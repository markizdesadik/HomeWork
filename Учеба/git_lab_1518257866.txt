на VM с установленной UBUNUTU
1) создать новую директорию subdir и перейти в нее. скачать с ftp-сервера архив
wget ftp://test:test@80.249.82.243/p.tar.gz
2) распаковать архив, наход€сь в свежесозданной директории subdir
tar -xzvf p.tar.gz
“еперь это ваша рабоча€ папка с проектом.
3) инициализировать репозиторий git в текущей папке (в которой уже находитс€ распакованный архив), сконфигурировать user.name и user.email (указывать фамилию и им€ в транслитерации).
4) »гнорируемые .gitignore файлы:
- любой архив tar.gz-архив
- файлы, наход€щиес€ в корневой директории репозитори€ и начинающиес€ на "testlog"
- js-файлы pr01.js, pr02.js, pr03.js ... pr09.js
- log-файлы в директории logs кроме all.log
- js-файлы в субдиректори€х, наход€щихс€ в директории modules, кроме js-файлов наход€щихс€ непосредственно в modules/
5) создать первый коммит под названием "gitignore created"
6) создать новую ветку testing и переключитьс€ в нее.
7) наход€сь в testing внести изменени€ в проект - удалить файлы pr11.js, pr12.js
8) закоммитить изменени€, название коммита "deleted some files"
9) на GITHUB создать репозиторий с именем "<group_id>-<mylastname>". <mylastname> - ваша фамили€. отредактировать README - написать что угодно, закоммитить в веб-интерфейсе.
10) вернутьс€ в реп в ubuntu.
11) добавить в ваш локальный репозиторий новый удаленный (на GITHUB) "<group_id>-<mylastname>"git br -v
git remote....
12) "забрать" с удаленного репозитори€ последние коммиты и влить их в локальный в ветку master
13) отправить в удаленный репозиторий свои коммиты из ветки master.

–абота в win8
14) открыть Git Bash и пройти в свою папку
15) склонировать, подчеркиваю, склонировать репозиторий с github на свою win8
16) “еперь, наход€сь в локальном репозитории в ветке master:
	перейти в ветку testing, 
	вернутьс€ в ветку master,
	влить ветку testing в master, закоммитить.
17) получить свежий коммит с github, отправить последний коммит из п.16 на github
18) прислать мне ссылку на ваш репозиторий в github (в netacad).




